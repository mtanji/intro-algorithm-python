# Q8

from math import pow								# 1.
user_input = input('informe um número inteiro: ')	# 2.
a = int(user_input)									# 3.
print('{0:.0f}'.format(pow(a,2)))					# 4.

# Enunciado modificado da questão 8

# Analise as afirmativas abaixo.
# I. Esse código está incorreto, exibindo um erro de leitura e escrita de dados.
# II. Na linha 2 e 4 estão os comandos básicos de leitura e escrita de dados.
# III. A linha 3 está sendo usada para declaração e inicialização da variável inteira a.
# IV. O código está calculando, na linha 4, o quadrado de um número inteiro.
# Está correto o que se afirma apenas em